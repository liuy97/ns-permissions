import { NsPermissions } from 'nativescript-ns-permissions';
console.log(new NsPermissions().message);
import { Component } from "@angular/core";

@Component({
    selector: "ns-app",
    templateUrl: "app.component.html"
})
export class AppComponent { }
